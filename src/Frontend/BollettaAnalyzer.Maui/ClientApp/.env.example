# Copia in .env.local per lo sviluppo.

# true = usa i dati mock, nessun backend richiesto (sviluppo UI)
VITE_USE_MOCK=false

# Base URL delle API. In dev il proxy di Vite gira /api -> http://localhost:5080
VITE_API_BASE_URL=/api
